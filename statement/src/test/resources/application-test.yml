spring:
  datasource:       #ввел для deal-test-container
    url: jdbc:postgresql://localhost:5432/test
    username: test
    password: test
  sql:
    init:
      mode: always
      schema-locations: classpath:schema.sql
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        default_schema: deal

server:
  port: 0

logging:
  level:
    root: WARN
calculator:
  base-url: http://localhost:${wiremock.server.port}    #конфиг не прописан, динамически назначаю calculator

deal:
  base-url: http://localhost:8090